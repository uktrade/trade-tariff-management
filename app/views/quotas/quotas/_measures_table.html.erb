<measures-grid :selected-rows="selectedMeasures" :disable-scroller="true" :on-item-selected="onMeasuresSelected" :on-item-deselected="onMeasuresDeselected" :data="measures" :columns="columns" :client-sorting="false" :on-select-all-changed="changeSelectionType" :selection-type="selectionType"></measures-grid>

<table-pagination :page="pagination.page" :total="pagination.total_count" :per-page="pagination.per_page" :on-click="onPageChange"></table-pagination>
