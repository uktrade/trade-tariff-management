<records-grid table-class="quotas-table" primary-key="quota_definition_sid" :selected-rows="selectedQuotas" :disable-scroller="true" :on-item-selected="onQuotasSelected" :on-item-deselected="onQuotasDeselected" :data="quotas" :columns="columns" :client-sorting="false" :on-select-all-changed="changeSelectionType" :selection-type="selectionType"></records-grid>

<table-pagination :page="pagination.page" :total="pagination.total_count" :per-page="pagination.per_page" :on-click="onPageChange"></table-pagination>
